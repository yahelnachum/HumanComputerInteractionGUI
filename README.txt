To run the program simply run main.java under the main package.

To register a user hit register and then you will be able to login with the new registered user by hitting login.

To login just hit the login button and enter an existing username and password.

To add to a wishlist you must be logged in first.

There is already an existing user with name=yahelnachum and password=123456 if you want to log into that one too.